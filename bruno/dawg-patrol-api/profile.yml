info:
  name: profile
  type: http
  seq: 7

http:
  method: GET
  url: http://localhost:8080/v1/api/profile
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
