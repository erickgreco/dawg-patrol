info:
  name: ws-user-telemetry
  type: websocket
  seq: 15

websocket:
  url: "{{base_url}}/v1/robots/{{robotID}}/{{reservationID}}/ws/user-telemetry"
  message:
    type: json
    data: "{}"
  auth:
    type: bearer
    token: "{{token}}"

settings:
  timeout: 0
  keepAliveInterval: 0
