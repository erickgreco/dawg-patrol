info:
  name: register-robot-differentRole
  type: http
  seq: 10

http:
  method: POST
  url: "{{base_url}}/v1/robots/register-robot"
  body:
    type: json
    data: |-
      {
        "serial_number": "09:AA:BB:C1:D3:A3",
        "name": "AstroR1",
        "battery": 70
      }
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
