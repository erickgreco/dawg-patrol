info:
  name: request-role-update
  type: http
  seq: 10

http:
  method: POST
  url: "{{base_url}}/v1/profile/request-role-update"
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
