info:
  name: login-second-user
  type: http
  seq: 5

http:
  method: POST
  url: http://localhost:8080/v1/login
  headers:
    - name: Content-Type
      value: application/json
  body:
    type: json
    data: |-
      {
        "email": "gopherx19@example.com",
        "password": "GopherX@2026"
      }
  auth: inherit

runtime:
  scripts:
    - type: after-response
      code: |-
        const data = res.getBody();

        bru.setEnvVar("token", data.data.token);

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
