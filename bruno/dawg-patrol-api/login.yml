info:
  name: login
  type: http
  seq: 4

http:
  method: POST
  url: http://localhost:8080/v1/login
  headers:
    - name: Content-Type
      value: application/json
  body:
    type: json
    data: |-
      {
        "email": "test@mail.com",
        "password": "testpassword"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
