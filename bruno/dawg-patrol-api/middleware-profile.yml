info:
  name: middleware-profile
  type: http
  seq: 7

http:
  method: GET
  url: "{{base_url}}/v1/home/profile"
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
