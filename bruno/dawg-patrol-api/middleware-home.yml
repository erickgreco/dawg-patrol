info:
  name: middleware-home
  type: http
  seq: 6

http:
  method: GET
  url: "{{base_url}}/v1/home"
  headers:
    - name: Authorization
      value: ""
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
