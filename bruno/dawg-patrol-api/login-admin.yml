info:
  name: login-admin
  type: http
  seq: 6

http:
  method: POST
  url: "{{base_url}}/v1/login"
  body:
    type: json
    data: |-
      {
        "email": "gopherx19@example.com",
        "password": "GopherX@2026"
      }
  auth: inherit

runtime:
  scripts:
    - type: after-response
      code: |-
        const data = res.getBody();

        bru.setEnvVar("token", data.data.token);
        bru.setEnvVar("refresh_token", data.data.refresh_token);

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
