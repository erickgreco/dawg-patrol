info:
  name: register-robot
  type: http
  seq: 9

http:
  method: POST
  url: "{{base_url}}/v1/robots/register-robot"
  body:
    type: json
    data: |-
      {
        "serial_number": "A1:B2:C3:D4:E5:F6",
        "name": "NoisyA1",
        "battery": 98
      }
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
