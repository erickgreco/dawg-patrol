info:
  name: test
  type: http
  seq: 6

http:
  method: GET
  url: http://localhost:8080/v1/api/home
  headers:
    - name: Authorization
      value: ""
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
