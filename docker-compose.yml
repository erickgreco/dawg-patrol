services:
  db:
    image: postgres:17.7
    container_name: postgres_db
    environment:
      POSTGRES_DB: dawg-patrol
      POSTGRES_USER: admin
      POSTGRES_PASSWORD: adminpassword
    volumes:
      - db-data:/var/lib/postgresql/data
    ports:
      - "5433:5432"
    
volumes:
  db-data: